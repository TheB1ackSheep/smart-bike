doctype html
html
    head
        block title
            title Default Smart Motocycle
        link(rel='stylesheet', href='/css/styles.css')           
    body
        nav
            div.header &nbsp;
            a(class=menu == 'dashboard' ? 'active' : '', href='/dashboard')
                div.icon.dashboard-icon
                span Dashboard
            a(class=menu == 'bikes' ? 'active' : '', href='/bikes')
                div.icon.bike-icon
                span Motocycles
            a(class=menu == 'safe-zones' ? 'active' : '', href='/safe-zones')
                div.icon.safe-zone-icon
                span Safe Zones                
            a(class=menu == 'history' ? 'active' : '', href='/history')
                div.icon.history-icon
                span History
        section.main-content
            block content
        block script          